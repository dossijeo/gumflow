function drawMenuHero(){screenTransform();const x=CW*.78,y=CH*.55,scale=Math.min(CW/250,CH/170);ctx.save();ctx.translate(x,y);ctx.rotate(-.15);ctx.strokeStyle='#79f7df29';ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(0,30,scale*55,scale*16,-.2,0,TAU);ctx.stroke();ctx.beginPath();ctx.ellipse(0,30,scale*68,scale*22,-.2,0,TAU);ctx.stroke();for(let i=0;i<6;i++){const a=visualT*.3+i*TAU/6;ctx.save();ctx.translate(Math.cos(a)*scale*54,Math.sin(a)*scale*36);ctx.rotate(a);ctx.fillStyle=i%2?'#d9ff8499':'#89eada99';rr(-scale*2,-scale*3,scale*4,scale*6,2);ctx.fill();ctx.restore()}ctx.fillStyle='#0c071833';ctx.beginPath();ctx.ellipse(0,scale*27,scale*23,scale*5,0,0,TAU);ctx.fill();ctx.scale(scale,scale);gum(0,Math.sin(visualT*2)*2,Math.sin(visualT*1.3)*.04,120,Math.sin(visualT*2)*.07,0,false,true);ctx.restore();ctx.textAlign='center';ctx.fillStyle='#fff4dbbb';ctx.font='800 10px system-ui';ctx.fillText('100% CHICLE. 0% SENTIDO COMÚN.',x,y+scale*45);ctx.textAlign='left'}
function render(){
 if(!level)return;background();worldTransform();drawProps();drawNPCs();drawGround();drawMaterials();drawPools();drawLoops();drawTubes(false);drawPlatforms();drawSigns();drawCheckpoints();drawFinish();drawBoosts();drawSprings();drawHazards();drawGadgets();drawSweets();drawCollectibles();drawEnemies();drawBossScene();if(state!=='menu'&&state!=='select')drawPlayer();drawTubes(true);drawEffects();if(state==='menu'||state==='select')drawMenuHero();else drawBanner();drawBossHUD();
 if(!reduced&&player&&Math.abs(player.vx)>1150&&state==='playing'){screenTransform();ctx.strokeStyle='#ffffff15';ctx.lineWidth=1;for(let i=0;i<10;i++){const x=((i*239-visualT*1300)%(CW+250)+CW+250)%(CW+250)-120,y=(i*131)%CH;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+75,y);ctx.stroke()}}
}

