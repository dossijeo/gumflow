function drawEffects(){for(const p of particles){ctx.globalAlpha=clamp(p.life/.5,0,1);ctx.fillStyle=p.color;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.life*4);rr(-p.size/2,-p.size/2,p.size,p.size,1);ctx.fill();ctx.restore()}ctx.globalAlpha=1;ctx.textAlign='center';for(const f of floaters){ctx.globalAlpha=clamp(f.life/.3,0,1);ctx.font='900 15px system-ui';ctx.lineWidth=4;ctx.strokeStyle='#171526';ctx.strokeText(f.text,f.x,f.y);ctx.fillStyle=f.color;ctx.fillText(f.text,f.x,f.y)}ctx.textAlign='left';ctx.globalAlpha=1}
function drawBanner(){if(bannerTime<=0||state!=='playing')return;screenTransform();ctx.globalAlpha=clamp(bannerTime,.0,1)*clamp((3.4-bannerTime)*2,0,1);const size=Math.min(CW/32,34);ctx.textAlign='center';ctx.font=`950 ${size*1.9}px system-ui`;ctx.fillStyle=level.palette.accent+'55';ctx.fillText('0'+(levelIndex+1),CW*.5,CH*.30-size*.8);ctx.font=`900 ${size}px system-ui`;ctx.fillStyle='#fff4db';ctx.fillText(level.name.toUpperCase(),CW*.5,CH*.30+size*.35);ctx.font=`600 ${Math.max(10,size*.38)}px system-ui`;ctx.fillStyle='#efe5f0';ctx.fillText(level.subtitle,CW*.5,CH*.30+size*1.2);ctx.textAlign='left';ctx.globalAlpha=1}
