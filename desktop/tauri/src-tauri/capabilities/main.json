{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the local game window may query or toggle native fullscreen.",
  "windows": ["main"],
  "permissions": ["core:window:allow-is-fullscreen", "core:window:allow-set-fullscreen"]
}
